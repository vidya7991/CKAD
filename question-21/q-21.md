🧩 Question 4: DaemonSet and Init Container

Task:

Create a DaemonSet named log-agent in namespace monitoring:

Image: busybox

Command: ["/bin/sh", "-c", "tail -f /var/log/containers/*.log"]

Runs on all nodes.

Include an initContainer named setup-dir that:

Uses image busybox

Runs: ["/bin/sh", "-c", "mkdir -p /var/log/containers && touch /var/log/containers/init.log"]

Mounts the same volume as the main container at /var/log/containers

Use an emptyDir volume named log-data for both containers.



to exec --> kubectl exec log-agent-2cgm8 -i -t -- /bin/sh