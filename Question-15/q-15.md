Advanced Pod Design: Init/Sidecar/Resource Limits (12 Minutes)
This question is a final check on Pod design principles, including resource constraints.

Create a Namespace named ckad-q5-advanced.

Create a Pod named complex-pod in the ckad-q5-advanced namespace.

Add an Init Container named pre-check (image busybox) that simply runs echo "Pre-check complete".

Add a multi-container main application structure:

Container A: Name main-app, Image nginx.

Container B (Sidecar): Name log-collector, Image busybox. Command: ["/bin/sh", "-c", "tail -f /dev/null"] (to keep it running).

Apply Resource Limits to the main-app container: CPU limit of 200m and Memory limit of 256Mi.

Verification: Use kubectl describe pod complex-pod to check that the Init container is listed, both main containers are defined, and the resource limits are applied correctly to the main-app.